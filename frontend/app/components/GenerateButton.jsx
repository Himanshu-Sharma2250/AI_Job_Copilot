import {generateApplication} from "@/lib/api"

const GenerateButton = () => {
    const handleClick = () => {
        
    }

    return (
        <div>
            <button className='border-2 px-5 py-2 cursor-pointer hover:bg-gray-300 hover:text-gray-900 rounded-xl'>
                Generate Application
            </button>
        </div>
    )
}

export default GenerateButton
